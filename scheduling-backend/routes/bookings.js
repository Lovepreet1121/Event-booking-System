// routes/bookings.js
// This file can be mostly empty for now as booking logic is moved

const express = require("express");
const router = express.Router();

// No booking logic here anymore, it's in events.js

module.exports = router;